# Vercel Postgres — copia los valores desde el dashboard de Vercel
# Storage → tu base de datos → .env.local
POSTGRES_URL=""
POSTGRES_PRISMA_URL=""
POSTGRES_URL_NON_POOLING=""
POSTGRES_USER=""
POSTGRES_HOST=""
POSTGRES_PASSWORD=""
POSTGRES_DATABASE=""

# Alpaca Market Data — https://app.alpaca.markets/paper/dashboard/overview
# (key paper o live, ambas funcionan para datos)
ALPACA_API_KEY_ID=""
ALPACA_API_SECRET_KEY=""

# Auth privado (solo Alan) — generar con:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DASHBOARD_PASSWORD=""
SESSION_SECRET=""
